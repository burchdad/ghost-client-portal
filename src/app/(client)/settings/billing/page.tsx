import { EmptyState } from "@/components/empty-state";

export default function BillingSettingsPage() {
  return (
    <EmptyState
      title="Billing settings"
      body="Billing contacts, cards, and subscription controls land with Stripe phases."
    />
  );
}
