import { EmptyState } from "@/components/empty-state";

export default function TeamSettingsPage() {
  return (
    <EmptyState
      title="Team settings"
      body="Client team management lands after invite flows are implemented."
    />
  );
}
