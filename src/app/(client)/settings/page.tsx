import { EmptyState } from "@/components/empty-state";

export default function SettingsPage() {
  return (
    <EmptyState
      title="Settings"
      body="Organization, team, and billing settings are protected route shells in Phase 1."
    />
  );
}
