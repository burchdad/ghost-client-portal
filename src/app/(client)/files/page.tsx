import { EmptyState } from "@/components/empty-state";

export default function FilesPage() {
  return (
    <EmptyState
      title="Files"
      body="Client-visible files and permissions land in Phase 4."
    />
  );
}
