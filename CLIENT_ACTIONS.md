# Client Actions

`ClientAction` tracks work needed from the client.

Actions include organization, project, optional assigned user, title, description, category, priority, status, due date, completion timestamp, related onboarding form, and client-visible instructions.

Actions appear on the dashboard and project workspace. Completion is handled server-side by workflow actions such as onboarding submission.
